name: Weather Weather Service CI/CD

on:
  push:
    paths:
      - 'src/weather/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check secrets
        run: |
          echo "USER=${{ secrets.DOCKER_USERNAME }}"
          echo "USER=${{ secrets.DOCKER_TOKEN }}"


      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
            username: ${{ secrets.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_TOKEN }}
            
      - name: Build Image
        run: |
          docker build -t elprogrammer1/weather-app-weather:${{ github.sha }} src/weather

      - name: Push Image
        run: |
          docker push elprogrammer1/weather-app-weather:${{ github.sha }}

      - name: Update K8s Deployment
        run: |
          sed -i "s|image:.*|image: elprogrammer1/weather-app-weather:${{ github.sha }}|" kubernetes/weather/deployment.yaml

    #   - name: Deploy
    #     run: kubectl apply -f kubernetes/weather/deployment.yaml
